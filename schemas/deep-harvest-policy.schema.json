{
  "$defs": {
    "SaturationPolicy": {
      "additionalProperties": false,
      "properties": {
        "minimum_iterations": {
          "default": 3,
          "minimum": 1,
          "title": "Minimum Iterations",
          "type": "integer"
        },
        "window": {
          "default": 3,
          "minimum": 1,
          "title": "Window",
          "type": "integer"
        },
        "novelty_threshold": {
          "default": 0.03,
          "maximum": 1,
          "minimum": 0,
          "title": "Novelty Threshold",
          "type": "number"
        }
      },
      "title": "SaturationPolicy",
      "type": "object"
    }
  },
  "additionalProperties": false,
  "properties": {
    "max_pages": {
      "default": 100,
      "maximum": 100000,
      "minimum": 1,
      "title": "Max Pages",
      "type": "integer"
    },
    "max_records": {
      "anyOf": [
        {
          "minimum": 1,
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Max Records"
    },
    "saturation": {
      "anyOf": [
        {
          "$ref": "#/$defs/SaturationPolicy"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    },
    "stop_on_error": {
      "default": true,
      "title": "Stop On Error",
      "type": "boolean"
    }
  },
  "title": "DeepHarvestPolicy",
  "type": "object"
}
