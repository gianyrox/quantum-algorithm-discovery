\section{Problem}
We estimate the dominant eigenvalue of a sparse matrix arising from a network model.
The main computational bottleneck is repeated matrix-vector multiplication at large dimension.

\begin{equation}
A x = \lambda x
\end{equation}
